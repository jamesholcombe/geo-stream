[package]
name = "stdin-stdout"
version = "0.1.0"
edition = "2021"
description = "NDJSON stdin/stdout adapter for geo-stream"

[dependencies]
engine = { path = "../../engine" }
geo = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
geojson = "0.24"
thiserror = "2"
